# Copy this file to .env and fill in your own values
# .env is in .gitignore so passwords are never pushed to GitHub

DB_HOST=localhost
DB_PORT=3306
DB_NAME=boardgame_tracker
DB_USER=root
DB_PASSWORD=your_password_here
